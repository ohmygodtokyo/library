<!DOCTYPE html>
<html>
  <head>
    <title>Library</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <% if user_signed_in? %>
      <% if current_page?(controller: 'translations', action: 'translate') %>
        <%= stylesheet_link_tag "translate", "data-turbo-track": "reload" %>
      <% else %>
        <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <% end %>
    <% else %>
      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <% end %>


      <%= stylesheet_link_tag "note", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      @import url('https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Poppins:ital,wght@0,100;0,300;0,400;0,500;0,600;0,700;1,100&display=swap');
    </style>

    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
  </head>

  <body>
    <% if user_signed_in? %>
      <% unless current_page?(controller: 'translations', action: 'translate') %>
        <%= render "shared/navbar" %>
      <% end %>
    <% end %>

    <%= render "shared/flashes" %>

    <%= yield %>
    <%= javascript_include_tag "languages" %>
    <%= javascript_include_tag "translate" %>
    <%= javascript_include_tag "notes" %>
  </body>
</html>
